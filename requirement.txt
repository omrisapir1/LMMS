# -----------------------------
# Core HF / ML stack
# -----------------------------

transformers==4.46.3
accelerate==0.33.0

tokenizers>=0.19.0,<0.21.0
safetensors>=0.4.3

# IMPORTANT:
# torch is installed separately per machine (CPU / GPU / nightly / source)
torch

# -----------------------------
# Data / utils
# -----------------------------

pyyaml>=6.0.1
datasets>=2.20.0
