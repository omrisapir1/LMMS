# -----------------------------
# Core HF / ML stack
# -----------------------------
# Needed for model_type "qwen3_5" support.
transformers==4.44.2
accelerate==0.33.0
tokenizers==0.22.2
safetensors>=0.4.3
bitsandbytes>=0.43.0

# -----------------------------
# Data / utils
# -----------------------------
pyyaml>=6.0.1
datasets==4.6.1
scikit-learn>=1.3.0

# -----------------------------
# Inference / eval
# -----------------------------
# Optional: install vLLM separately in a compatible env (Python 3.10-3.13).
# pip install "vllm==0.16.0"
