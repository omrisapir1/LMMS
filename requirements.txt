# -----------------------------
# Core HF / ML stack
# -----------------------------
# Qwen 3.5 + current vLLM-compatible stable range
ransformers >= 5.2.0
accelerate>=0.33.0
safetensors>=0.4.3
bitsandbytes>=0.43.0
huggingface-hub>=0.23.2,<1.0

# IMPORTANT:
# Do NOT pin tokenizers manually.
# Let pip resolve a compatible tokenizers version for the selected transformers.

# -----------------------------
# Data / utils
# -----------------------------
pyyaml>=6.0.1
datasets>=4.6.1
scikit-learn>=1.3.0

# -----------------------------
# Inference / eval
# -----------------------------
# Optional: install vLLM separately in a compatible env (Python 3.10-3.13).
# pip install "vllm==0.16.0"
