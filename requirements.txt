# -----------------------------
# Core HF / ML stack
# -----------------------------
transformers==4.57.6
accelerate==0.33.0
tokenizers==0.22.2
safetensors>=0.4.3

# -----------------------------
# Data / utils
# -----------------------------
pyyaml>=6.0.1
datasets==4.6.1
scikit-learn>=1.3.0

# -----------------------------
# Inference / eval
# -----------------------------
# Optional: install vLLM separately in a compatible env (Python 3.10-3.13).
# pip install "vllm==0.16.0"
