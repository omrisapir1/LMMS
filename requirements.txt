# -----------------------------
# Core HF / ML stack
# -----------------------------
# Qwen 3.5 + current vLLM-compatible stable range
git+https://github.com/huggingface/transformers.git@main

accelerate>=0.33.0
safetensors>=0.4.3
bitsandbytes>=0.43.0
huggingface-hub>=0.36.0,<2.0

# DO NOT pin tokenizers

# -----------------------------
# Data / utils
# -----------------------------
pyyaml>=6.0.1
datasets>=4.6.1
scikit-learn>=1.3.0

# -----------------------------
# Inference / eval
# -----------------------------
# Optional: install vLLM separately in a compatible env (Python 3.10-3.13).
# pip install "vllm==0.16.0"
